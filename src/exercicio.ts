function calculeArea(base: number, altura: number): number {
    return base * altura;
}


function dizOla(nome: string): string {
    return "Olá " + nome;
}

console.log(calculeArea(10,20));
console.log(dizOla("Matheus")); 